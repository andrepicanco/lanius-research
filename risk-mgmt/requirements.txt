pandas>=2.0
numpy>=1.24
PyYAML>=6.0
matplotlib>=3.8
requests>=2.31
pytest>=7.4
openpyxl>=3.1

# Only needed for --mode live / --price-source mt5 - the MetaTrader5 package only
# installs and works on Windows, against an already-open desktop terminal. Not needed
# for --mode local (backtest log analysis), which is what a GitHub-hosted CI runner uses.
# MetaTrader5>=5.0.45
